Позволяет фильтровать строки выборки по условиям

Чаще всего используется с операторами сравнения:

![[Логические операторы#^compare]]

Например:
```sql
SELECT *
FROM table1
WHERE col1 > 0
```
Вернёт выборку из строк, в которых `col1` больше нуля