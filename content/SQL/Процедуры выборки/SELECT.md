Создаёт выборку из указанных столбцов

```sql
SELECT col1, col2
FROM table1
```

Может принимать символ "*" для выборки из всех стобцов

```sql
SELECT *  -- То же самое что перечислить все столбцы
FROM table1
```